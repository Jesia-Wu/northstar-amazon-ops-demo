# 001：实施任务清单

> 状态：第一期本地演示功能已完成；现行托管与发布见 `../005-insforge-publishing/`。

## 任务使用规则

- 每个任务完成前必须附上对应 AC 的新鲜验证结果，并更新根目录 `task.md`。
- 涉及业务规则、状态机、字段映射、Schema 时先写失败测试，再写实现。
- 本清单不是完成证明；勾选之前需要真的跑过验证，纸上谈兵的勾没有加成。

## 0. 规格与 UI 冻结（当前阶段）

- [x] T0.1 创建标准项目 V5 工件并登记冻结范围。  
  验证：`AGENTS.md`、`task.md`、`spec.md`、`plan.md`、`tasks.md`、`.codex/v5-project.json` 存在。
- [x] T0.2 用户确认初级网站不接 MCP、使用内置演示数据。  
  验证：`AGENTS.md`、`spec.md`、`plan.md` 已更新为无网络/无凭据边界。
- [x] T0.3 用户通过可视化草图选择 A：证据优先工作台。  
  验证：`ui-design.md` 记录视觉系统、布局、状态和无障碍规则。
- [x] T0.4 确认初级版本为本地单页演示，不做认证和云端部署。  
  验证：规格的角色/权限和冻结接口已更新。
- [x] T0.5 将初级网站扩展为新品运营闭环控制台，并冻结 Precision Glass Console 视觉方向。  
  验证：`spec.md`、`ui-design.md`、`plan.md` 增加 AC-015/016 与模块、动效规则。
- [x] T0.6 按用户反馈将视觉方向更正为 Summer Glass Operations。  
  验证：`ui-design.md` 记录冰水远景、冰白水蓝玻璃与背景使用权提示；此变更不改变无 MCP、本地演示边界。

## 1. 应用基础与任务域

- [x] T1.1 初始化 Vite/React/TypeScript 项目、格式化和测试运行时。  
  验证：健康页可打开；测试命令、类型检查、构建命令记录到 `AGENTS.md`。
- [x] T1.2 先写 ASIN 校验、站点限制、目标枚举和任务状态机测试。  
  验证：非法 ASIN/目标/站点、所有合法状态转换均有失败与通过案例（AC-001、AC-002）。
- [x] T1.3 实现浏览器内存演示状态、步骤事件与本地重置。  
  验证：组件测试可创建本地研究、生成步骤事件、刷新后回到初始状态（AC-001、AC-003）。
- [x] T1.4 实现输入表单与演示状态启动交互。  
  验证：组件测试覆盖合法输入、格式错误与禁用状态（AC-001 至 AC-003）。

## 2. 内置演示资料与证据链

- [x] T2.1 实现固定 `demoResearch` fixture 与证据编号。  
  验证：fixture 覆盖产品、图片、Brief、行动包和 data gap（AC-004 至 AC-008）。
- [x] T2.2 实现演示步骤事件与可见“内置演示”标识。  
  验证：组件测试断言顺序、状态和误导性文案不存在（AC-003、AC-011）。
- [x] T2.3 实现产品档案、图片资产和字段 provenance 呈现。  
  验证：指定产品字段可回溯 evidence ID/JSON path；缺字段明确显示（AC-004 至 AC-006）。
- [x] T2.4 实现图片资产及其证据编号/演示状态。  
  验证：顺序、证据编号和 data gap 通过 UI 测试（AC-007）。

## 3. 证据约束的 AI 风格演示与运营草稿

- [x] T3.1 先写 Brief fixture 完整性、citation 显示和数据不足规则测试。
  验证：`src/data/demoResearch.test.ts` 断言所有可见建议引用已知证据，并显式列出未提供的数据（AC-008）。
- [x] T3.2 实现 Brief 展示及证据交互。  
  验证：组件测试和数据不足状态通过（AC-008）。
- [x] T3.3 实现 Listing/广告/图片行动包、草稿标记与复制。  
  验证：三类建议均为 `draft`，复制反馈可见，不含写入调用（AC-009、AC-010、AC-013）。
- [x] T3.4 实现禁用导出说明与外部连接零请求检查。  
  验证：浏览器网络和组件测试验证不存在 MCP/Amazon/LLM 请求（AC-012）。

## 4. 工作台、导出与验收

- [x] T4.1 实现 Summer Glass 运营控制台：模块导航、阶段、今日任务、输入、时间线和输入错误交互。  
  验证：浏览器测试覆盖创建、进度、输入错误（AC-003、AC-011）。
- [x] T4.2 实现产品研究、Listing、广告、创意、利润视图与产品档案、图片资产、证据、Brief 和行动包 UI。  
  验证：浏览器测试和人工检查字段来源/时间可见（AC-004 至 AC-009）。
- [x] T4.3 实现复制草稿和禁用导出控件。  
  验证：复制反馈、禁用解释和零外部请求通过测试（AC-010、AC-012）。
- [x] T4.4 完成 E2E、可访问性基础检查和验收矩阵。  
  验证：AC-001 至 AC-016 每项均映射到测试或人工证据；结果写入 `task.md`。

## 5. 发布前检查（仅 MVP 可用后）

- [x] T5.1 用户试用初级网站，确认工作台布局、内容层级与演示交互。
  验证：2026-07-16 正式站浏览器验收走通控制台与创意资产核心路径。
- [x] T5.2 决定暂不进入真实数据版，继续保持本地 fixture、无账号和无外部数据调用；未来接入需另建高风险功能包。
