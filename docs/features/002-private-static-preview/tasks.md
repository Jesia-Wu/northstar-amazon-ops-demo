# 002：私有静态试用站任务清单

> 状态：历史发布记录，已退役；当前正式托管见 `../005-insforge-publishing/`。

- [x] T0.1 冻结私有静态发布范围、访问策略、回滚和验收标准。  
  验证：`spec.md`、`plan.md`、`tasks.md` 与项目状态已更新。
- [x] T1.1 增加静态 Worker 入口和发布构建脚本。  
  验证：`npm run build:site` 生成 `dist/server/index.js`。
- [x] T1.2 初始化受控源代码历史与托管元数据。  
  验证：提交 SHA 可用于保存版本；托管元数据不含凭据。
- [x] T2.1 构建、打包并保存与提交匹配的版本。  
  验证：构建和包校验通过，Sites 返回版本 ID。
- [x] T2.2 尝试 owner-only 私有部署并轮询结果。  
  验证：部署成功返回可打开地址；若不能私有部署则停止并记录原因。
- [x] T2.3 修复托管产物布局并发布修复版本。  
  验证：先测试 `dist/client/index.html` + `dist/server/index.js` 结构，再以私有诊断访问根路径确认不再返回空 404（AC-201、AC-204、AC-206）。
- [x] T3.1 回写实际发布证据与未扩大边界。  
  验证：`task.md` 记录 URL 状态、版本证据、访问策略和下一步。
